# Android STT 전처리용 노이즈 저감(Noise Reduction) 알고리즘 정리

> 작성일: 2026-08-26
> 대상 환경: Android Application (on-device)
> STT 엔진: **Google Embedded — ML Kit GenAI Basic (API 31+)**
> 사용 시나리오: 음원을 **10초 단위**로 끊어 STT 전사 → 회의록 / 대화 요약 / 하루 기록 / 일정·약속 잡기 / 스케줄 반영
> 소음 종류: 카페 소음(babble), 도로 소음, 음악 소음, 키보드 타자음, 마우스 클릭음

---

## 1. 한눈에 보기 (요약 비교표)

| # | 알고리즘 / 라이브러리 | 유형 | 라이선스 | 비용 | 성능(비정상 소음 기준) | Android 적용 | 난이도 | 실시간성 | 입력 형식 | VAD 포함 |
|---|----------------------|------|----------|------|------------------------|--------------|--------|----------|-----------|----------|
| 1 | **DeepFilterNet** | 딥러닝(DSP+DNN) | MIT / Apache-2.0 | 무료 | ★★★★★ | ONNX Runtime | 중~높음 | 저지연 실시간(~40ms) | 48kHz PCM | ✗ (별도) |
| 2 | **GTCRN** | 딥러닝(경량 CRN) | Apache-2.0 | 무료 | ★★★★☆ | ONNX Runtime | 중 | 실시간(초경량) | 16kHz PCM | ✗ (별도) |
| 3 | **NSNet2** (MS DNS) | 딥러닝 | MIT | 무료 | ★★★★☆ | ONNX Runtime | 중 | 실시간 | 16kHz PCM | ✗ (별도) |
| 4 | **FRCRN / DCCRN** | 딥러닝 | Apache-2.0 | 무료 | ★★★★☆ | ONNX / TFLite | 높음 | 준실시간(무거움) | 16kHz PCM | ✗ |
| 5 | **Demucs(denoiser)** | 딥러닝 | MIT | 무료 | ★★★★☆ | TFLite / ONNX | 높음 | 실시간 부담 | 16kHz PCM | ✗ |
| 6 | **RNNoise** | 하이브리드(DSP+소형 RNN) | BSD | 무료 | ★★★☆☆ | NDK/JNI(C) | 낮음 | 매우 빠름 | 48kHz, 480샘플/프레임 | 부분(내부 VAD 특징) |
| 7 | **WebRTC APM** | DSP 통합 스택 | BSD | 무료 | ★★★☆☆ | NDK/JNI(C++) | 중 | 실시간 | 10ms 프레임 | ✔ (VAD 내장) |
| 8 | **NoiseSuppressor (OS 내장)** | 하드웨어/DSP | OS 내장 | 무료 | ★★☆☆☆ | audiofx API | 매우 낮음 | 실시간 | audioSessionId | ✗ |
| 9 | **AcousticEchoCanceler (OS 내장)** | 하드웨어/DSP | OS 내장 | 무료 | ★★☆☆☆(에코 전용) | audiofx API | 매우 낮음 | 실시간 | audioSessionId | ✗ |
| 10 | **Spectral Subtraction** | 전통 DSP | 자체 구현 | 무료 | ★★☆☆☆ | 직접 구현 | 낮음 | 매우 빠름 | STFT 프레임 | ✗ |
| 11 | **Wiener Filter** | 전통 DSP | 자체 구현 | 무료 | ★★☆☆☆ | 직접 구현 | 낮음 | 매우 빠름 | STFT 프레임 | ✗ |
| 12 | **MMSE-STSA (Ephraim-Malah)** | 전통 통계 DSP | 자체 구현 | 무료 | ★★★☆☆ | 직접 구현 | 중 | 빠름 | STFT 프레임 | ✗ |
| 13 | **Spleeter** | 소스 분리 | MIT | 무료 | ★★★★☆(음악 분리) | TFLite | 높음 | 오프라인 지향 | 44.1kHz | ✗ |
| 14 | **Conv-TasNet / SepFormer** | 소스 분리 | Apache-2.0 | 무료 | ★★★★☆ | ONNX/TFLite | 높음 | 준실시간 | 8/16kHz | ✗ |

> ★ 성능은 **비정상(non-stationary) 소음 + babble(웅성거림) + 음악** 환경 기준 상대 평가입니다. 정상 소음(팬, 도로 지속음)만 놓고 보면 전통 DSP도 체감 성능이 올라갑니다.

---

## 2. 알고리즘별 상세

### 2.1 DeepFilterNet (ONNX Runtime)
- **특징**: STFT/ERB 특징 + 다단계 DNN. 저지연 실시간 설계. 현재 무료 실시간 NS 중 품질 최상위권.
- **라이선스 / 비용**: MIT·Apache-2.0 / 무료(상업적 사용 가능)
- **Android 적용**: `com.microsoft.onnxruntime:onnxruntime-android`
- **난이도**: 중~높음 (프레이밍·STFT·ISTFT 전후처리 직접 구현 필요할 수 있음)
- **실시간성**: 저지연(~40ms), 실시간 가능
- **입력**: 48kHz PCM
- **장점**: 카페 babble·음악·클릭음까지 제거 성능 우수
- **단점**: 연산량이 RNNoise보다 큼(저사양 기기 부하 확인 필요), 파이프라인 복잡
- **VAD**: 미포함(별도 Silero VAD 권장)

### 2.2 GTCRN (ONNX Runtime) — ★모바일 최적화 강추
- **특징**: 약 23K 파라미터의 초경량 CRN. 모바일 실시간이 설계 목적.
- **라이선스 / 비용**: Apache-2.0 / 무료
- **Android 적용**: ONNX Runtime Mobile
- **난이도**: 중
- **실시간성**: 실시간(연산량 매우 적음)
- **입력**: 16kHz PCM (STT와 샘플레이트 일치 → 리샘플 불필요)
- **장점**: DeepFilterNet에 근접한 품질을 훨씬 적은 연산으로 달성, 저사양 기기 친화
- **단점**: 극단적 소음에선 DeepFilterNet 대비 약간 열세
- **VAD**: 미포함

### 2.3 NSNet2 (ONNX Runtime)
- **특징**: Microsoft DNS Challenge 베이스라인, 순수 신경망 구조 단순.
- **라이선스 / 비용**: MIT / 무료
- **난이도**: 중 (ONNX export·이식 용이)
- **실시간성**: 실시간
- **입력**: 16kHz PCM
- **장점**: 구조 단순·이식 쉬움, 품질 양호
- **단점**: 최신 모델 대비 품질 다소 낮음
- **VAD**: 미포함

### 2.4 FRCRN / DCCRN
- **특징**: 복소 스펙트럼 기반 고품질 NS(ModelScope 계열).
- **라이선스 / 비용**: Apache-2.0 / 무료
- **난이도**: 높음 (모델 변환·최적화 필요)
- **실시간성**: 준실시간(무거움 → 중고급 기기)
- **입력**: 16kHz PCM
- **장점**: 품질 높음
- **단점**: 연산량 큼, 저사양 부담
- **VAD**: 미포함

### 2.5 Demucs(denoiser, Facebook)
- **특징**: 파형 도메인 딥러닝 denoiser.
- **라이선스 / 비용**: MIT / 무료
- **난이도**: 높음
- **실시간성**: 실시간엔 부담
- **입력**: 16kHz PCM
- **장점**: 품질 우수
- **단점**: 무거움
- **VAD**: 미포함

### 2.6 RNNoise (NDK/JNI, C)
- **특징**: Xiph.org 하이브리드 DSP + 소형 GRU.
- **라이선스 / 비용**: BSD / 무료
- **Android 적용**: CMake 빌드 + JNI 래퍼
- **난이도**: 낮음(통합 쉬움)
- **실시간성**: 매우 빠름
- **입력**: **48kHz, 480샘플(10ms) 고정** → 16kHz STT와 **리샘플 파이프라인 필요**
- **장점**: 초경량, 빠른 프로토타입
- **단점**: 모델이 작아 음악·강한 babble엔 한계
- **VAD**: 내부적으로 음성 확률 특징 사용(부분적)

### 2.7 WebRTC Audio Processing Module (APM)
- **특징**: NS + AEC + AGC + VAD + HighPassFilter 통합 스택.
- **라이선스 / 비용**: BSD / 무료
- **Android 적용**: `audio_processing` 모듈만 NDK 빌드 + JNI
- **난이도**: 중
- **실시간성**: 실시간
- **입력**: 10ms 프레임 (16/48kHz)
- **장점**: **기기 독립적**(제조사 편차 없음), 검증된 안정성, VAD·AGC 통합으로 파이프라인 단순화
- **단점**: 전통 DSP라 음악 등 비정상 소음엔 딥러닝보다 약함
- **VAD**: ✔ 내장

### 2.8 NoiseSuppressor (Android 내장, audiofx)
- **특징**: `android.media.audiofx.NoiseSuppressor`. 정상 소음에 유효.
- **라이선스 / 비용**: OS 내장 / 무료
- **난이도**: 매우 낮음(수 줄)
- **실시간성**: 실시간
- **입력**: `audioSessionId`
- **장점**: 구현 최소, 항상 켤 수 있는 최소 방어선
- **단점**: 제조사 구현 의존(기기 편차 큼), 비정상 소음에 약함
- **VAD**: 미포함

### 2.9 AcousticEchoCanceler (Android 내장)
- **특징**: 스피커→마이크 에코 제거. **STT 전사보다는 통화용**.
- **라이선스 / 비용**: OS 내장 / 무료
- **난이도**: 매우 낮음
- **실시간성**: 실시간
- **장점**: 에코 상황(스피커 재생 동시 녹음)에 유효
- **단점**: 일반 배경소음 제거 효과는 제한적
- **VAD**: 미포함

### 2.10~2.12 전통 DSP (Spectral Subtraction / Wiener / MMSE-STSA)
- **특징**: STFT 상에서 소음 스펙트럼 추정 후 감산·필터링.
- **라이선스 / 비용**: 자체 구현 / 무료
- **난이도**: 낮음~중
- **실시간성**: 매우 빠름
- **입력**: STFT 프레임
- **장점**: 초경량, 폴백·결합용으로 유용
- **단점**: 뮤지컬 노이즈 아티팩트(특히 Spectral Subtraction), 비정상 소음·음악에 취약
- **VAD**: 미포함(소음 추정용 간이 VAD 병행 가능)

### 2.13~2.14 소스 분리 (Spleeter / Conv-TasNet / SepFormer)
- **특징**: 음성과 음악/타 화자를 **분리**. 음악 소음이 매우 강할 때 고려.
- **라이선스 / 비용**: MIT / Apache-2.0 / 무료
- **난이도**: 높음
- **실시간성**: Spleeter는 오프라인 지향, TasNet 계열은 준실시간
- **장점**: 음악·중첩 음성 분리에 특화
- **단점**: 무겁고 통합 복잡, 실시간 10초 파이프라인엔 과함
- **VAD**: 미포함

---

## 3. 시나리오 특성 분석 (본 프로젝트)

| 요소 | 값 | 파이프라인 영향 |
|------|----|-----------------|
| STT 엔진 | ML Kit GenAI Basic (on-device, API 31+) | 자체 강건성 있음 → 과도한 NS는 오히려 WER 악화 위험 |
| 세그먼트 | 10초 단위 | **엄밀한 프레임 저지연 불필요** → 스트리밍 실시간성 부담 적음, 품질 우선 가능 |
| 목적 | 회의록·요약·기록·일정·스케줄 | **키워드(시간/날짜/사람/장소) 정확도**가 중요 → babble·음악 억제 효과 큰 모델 유리 |
| 소음 | babble·도로·음악·타자·클릭 | 대부분 **비정상 소음** → 전통 DSP 단독은 부족, 딥러닝 우위 |
| 기기 | on-device Android | 배터리·발열·저사양 고려 → 경량 모델 선호 |

> **핵심 포인트**: 10초 배치 처리이므로 초저지연이 필수가 아닙니다. 따라서 "실시간 스트리밍 최적화"보다 **세그먼트 단위 품질**이 좋은 경량 딥러닝 모델이 가장 잘 맞습니다.

---

## 4. 권장 우선순위 (본 프로젝트 최적안)

### 🥇 1순위 — GTCRN (ONNX Runtime)
- **이유**: 16kHz 입력이라 ML Kit STT와 **샘플레이트 일치(리샘플 불필요)**, 초경량으로 on-device 배터리·발열 부담 최소, 비정상 소음 제거 품질 우수.
- **적합성**: 10초 배치·저사양 기기·일정/키워드 정확도 요구에 균형 최적.

### 🥈 2순위 — DeepFilterNet (ONNX Runtime)
- **이유**: 소음이 매우 심한 환경(강한 음악·복잡한 카페)에서 **최고 품질**. 10초 배치라 연산량 여유를 감당 가능.
- **주의**: 48kHz ↔ 16kHz 리샘플 필요, 연산량 큼 → 중급 이상 기기 권장.

### 🥉 3순위 — WebRTC APM (baseline 겸 전처리)
- **이유**: NS+AGC+**VAD** 통합. **VAD로 비음성 10초 구간을 STT 호출 전에 스킵** → 배터리·비용 절감. AGC로 음량 정규화하면 STT 인식 안정화.
- **활용**: 딥러닝 NS 앞단의 **경량 전처리 + 무음 구간 필터**로 병용 추천.

### 4순위 — NoiseSuppressor(내장) + `VOICE_RECOGNITION` 소스
- **이유**: 구현 최소의 **항상 켜는 최소 방어선**. 단, 딥러닝 NS와 중복 시 음성 뭉개짐 가능 → 실측 후 택일.

### 참고(비권장/특수)
- **RNNoise**: 빠른 프로토타입엔 좋으나 음악 소음 한계 → 본 시나리오 주력으론 부적합.
- **소스 분리(Spleeter 등)**: 음악이 지배적일 때만 특수 목적으로. 실시간 10초 파이프라인엔 과함.
- **전통 DSP**: 딥러닝 실패 시 폴백/결합용.

---

## 5. 권장 파이프라인

```
마이크(AudioSource.VOICE_RECOGNITION, 16kHz)
        │
        ▼
[선택] WebRTC APM  ── HighPass + AGC + VAD
        │            └─ VAD 결과: 비음성 10초 세그먼트는 STT 스킵
        ▼
GTCRN (ONNX, 16kHz)  ── 주력 노이즈 저감  (환경 극심 시 DeepFilterNet로 교체)
        │
        ▼
[선택] Silero VAD  ── 음성 구간 정밀 트리밍
        │
        ▼
10초 세그먼트 버퍼링
        │
        ▼
ML Kit GenAI Basic STT (on-device)
        │
        ▼
후처리: 회의록/요약/일정·스케줄 추출
```

### 마이크 소스 및 내장 이펙트 예시(Kotlin)
```kotlin
val audioRecord = AudioRecord(
    MediaRecorder.AudioSource.VOICE_RECOGNITION, // STT용 권장(왜곡 최소)
    16_000, AudioFormat.CHANNEL_IN_MONO,
    AudioFormat.ENCODING_PCM_16BIT, bufferSize
)

// 최소 방어선(딥러닝 NS와 중복 적용 시 A/B 후 결정)
if (NoiseSuppressor.isAvailable()) {
    NoiseSuppressor.create(audioRecord.audioSessionId)?.enabled = true
}
```

### ONNX Runtime 의존성(GTCRN / DeepFilterNet)
```kotlin
// app/build.gradle.kts
dependencies {
    implementation("com.microsoft.onnxruntime:onnxruntime-android:1.20.0")
}
```

---

## 6. 반드시 지킬 실무 원칙

1. **과도한 노이즈 제거 금지**: ML Kit STT 자체가 잡음에 어느 정도 강건. 강한 NS는 음성 아티팩트를 만들어 **WER를 오히려 악화**시킬 수 있음.
2. **WER A/B 검증 필수**: (원음) vs (NS 적용) vs (NS+VAD)를 실제 오디오로 단어 오류율 비교 후 강도 결정.
3. **10초 배치 이점 활용**: 초저지연 불필요 → 품질 좋은 경량 딥러닝 우선, VAD로 무음 세그먼트 스킵해 비용 절감.
4. **샘플레이트 일치**: 가능하면 16kHz 계열(GTCRN) 사용해 리샘플 오버헤드·품질 손실 최소화.
5. **가속기 활용**: ONNX Runtime의 NNAPI / XNNPACK EP로 CPU 부하·발열 경감.
6. **중복 NS 주의**: 내장 NoiseSuppressor + 딥러닝 NS 동시 적용은 음성 뭉개짐 유발 가능 → 택일 또는 실측.

---

## 7. 최종 결론 (TL;DR)

- **주력: GTCRN(ONNX)** — 16kHz·초경량·on-device 최적, 본 시나리오 1순위.
- **고소음 대안: DeepFilterNet(ONNX)** — 최고 품질, 중급+ 기기.
- **전처리/무음필터: WebRTC APM(VAD·AGC 내장)** 병용 권장.
- **최소 방어선: 내장 NoiseSuppressor + VOICE_RECOGNITION**.
- 모든 선택은 **WER A/B 테스트**로 최종 확정.
```
