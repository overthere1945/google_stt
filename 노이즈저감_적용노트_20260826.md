# 노이즈 저감(Noise Reduction) 적용 노트 — 2026-08-26

대상 저장소: `https://github.com/overthere1945/google_stt.git`
기준 커밋: `83782d6  2026-08-26 15:50  ADD Silero VAD`
작성: hyungchul-20260826-1100

---

## 0. 먼저 알아야 할 것 — 요청하신 8종의 현실

**요청하신 8종 중 3종만 실제로 이 앱에 넣을 수 있습니다.** 나머지 5종은 코드를 잘 짜고 못 짜고의 문제가 아니라, 파일 입력 방식에 **원천적으로 적용이 불가능**하거나 **배포된 모델이 존재하지 않습니다.**

추측이 아니라 **실제 .onnx 파일을 내려받아 onnxruntime 으로 열어서** 확인한 결과입니다.

| 순위 | 요청 | 판정 | 근거 |
|---|---|---|---|
| **1** | **GTCRN** | ✅ **구현 완료** | `gtcrn_simple.onnx` 523 KB, MIT, 16 kHz 네이티브. 파일을 받아 I/O 를 직접 확인했고 실제 음원으로 돌려 6 dB 잡음 감쇠를 측정했습니다 |
| **2** | **DeepFilterNet** | ⚠️ **DPDFNet 으로 대체** | 업스트림은 **48 kHz 전용 + ONNX 3개로 분리 + 공식 export 배포본 없음**. 같은 계열(DeepFilterNet2 개선판)인 **DPDFNet**(CEVA, Apache-2.0)은 16 kHz 네이티브 단일 ONNX 라 그대로 씁니다. 실측 잡음 감쇠 **67.7 dB** |
| **3** | WebRTC APM | ❌ **불가 (NDK 필요)** | Kotlin 에서 APM 의 노이즈 억제만 오프라인 버퍼에 적용하는 **Maven 아티팩트가 존재하지 않습니다.** Maven Central 의 webrtc 패키지는 전부 PeerConnection 전체 스택이고, 유일하게 Java API 를 노출하는 `webrtc-java` 는 **Android ABI 가 없습니다**(Windows/macOS/Linux 전용) |
| **4** | **NSNet2** | ✅ **구현 완료** | `nsnet2-20ms-baseline.onnx` 10.3 MB, MIT. 공식 저장소 `master` 가 아니라 **`icassp2021-final` 브랜치**에 있습니다 |
| **5** | NoiseSuppressor (OS) | ❌ **원천 불가** | `android.media.audiofx.NoiseSuppressor` 는 생성자가 `create(audioSession)` **하나뿐**이고 그 세션은 **살아 있는 AudioRecord** 에서만 나옵니다. 공식 문서도 "캡처되는 신호에 붙는 audio pre-processor" 라고 못박습니다. ByteArray/파일을 넣는 API 가 **아예 없습니다** |
| **6** | Conv-TasNet / SepFormer | ❌ **불가** | 16 kHz 음성 향상용 ONNX 배포본 없음(SpeechBrain 은 ONNX export 미지원). SepFormer 는 25.7 M 파라미터 / **69.6 GMAC-per-second** → 85초 파일이면 약 5.9 TMAC 이고, 시간영역 transformer 라 중간 텐서만 수백 MB 여서 휴대폰에서 **OOM** |
| **7** | Spleeter | ❌ **불가** | 애초에 denoiser 가 아니라 **44.1 kHz 음악 스템 분리기**(보컬/드럼/베이스)입니다. "노이즈"라는 개념이 없습니다. 공식 SavedModel 은 TFLite 변환 불가, 커뮤니티 변환본은 **약 150 MB + GPU 가속 불가 + ONNX 아님** |
| **8** | FRCRN / DCCRN | ❌ **불가** | 내려받을 수 있는 ONNX 배포본이 없습니다. 두 모델 다 **복소 convolution / 복소 LSTM** 을 쓰는데 이 연산이 `torch.onnx.export` 에서 막혀서, 복소 연산을 실수 2채널로 다시 쓰지 않으면 변환 자체가 안 됩니다 |

### 그래서 빈자리를 이렇게 채웠습니다

첨부하신 조사 문서의 **10~12번(전통 DSP)** 을 순수 Kotlin 으로 구현해 넣었습니다.

| 추가 | 근거 문헌 | 특징 |
|---|---|---|
| **MMSE-STSA** | Ephraim & Malah 1984 (식 7·8·51) | 전통 DSP 중 musical noise 가 가장 적어 ASR 앞단에 무난 |
| **Wiener decision-directed** | Scalart & Vieira Filho 1996 | MMSE 와 앞단이 완전히 같고 이득식만 달라 A/B 짝으로 최적 |
| **Spectral Subtraction** | Berouti 1979 (과감산 + 스펙트럼 바닥) | 가장 빠름. musical noise 는 가장 심함 |

**모델 파일이 전혀 필요 없습니다.** 아무것도 안 받아도 오늘 바로 A/B 비교를 시작할 수 있습니다.

**불가 5종도 UI 목록에는 그대로 남겨 뒀습니다.** 고르면 위 표의 사유가 화면 로그에 바로 뜹니다.
"왜 안 되는지"가 앱 안에 기록으로 남게 하려는 의도입니다.

---

## 1. 최종 알고리즘 목록 (UI 스피너 순서)

```
사용 안 함 (기존 baseline)                          ← 기본값
1. GTCRN (ONNX, 16 kHz, 523 KB)                      ← 1순위, zip 에 모델 포함
2. DPDFNet — DeepFilterNet 계열 (ONNX, 16 kHz)        ← 2순위 대체
4. NSNet2 / MS DNS (ONNX, 16 kHz, 10 MB)             ← 4순위
MMSE-STSA (Kotlin, 모델 불필요)
Wiener decision-directed (Kotlin, 모델 불필요)
Spectral Subtraction (Kotlin, 모델 불필요)
3. WebRTC APM — 파일 입력 불가                        ← 고르면 사유 표시
5. NoiseSuppressor (OS 내장) — 파일 입력 불가
6. Conv-TasNet / SepFormer — 파일 입력 불가
7. Spleeter — 파일 입력 불가
8. FRCRN / DCCRN — 파일 입력 불가
```

### 알고리즘별 옵션

알고리즘을 바꾸면 **그 알고리즘의 권장값으로 옵션이 다시 채워지고, 쓰지 않는 칸은 회색으로 비활성화**됩니다.
(값을 넣어도 결과가 안 바뀌는 칸을 만지는 일이 없도록)

| 옵션 | 쓰는 알고리즘 | 기본값 | 의미 |
|---|---|---|---|
| **Attenuation Limit (dB)** | 전부 | 딥러닝 12 / DSP 0 | ★ **과도한 제거 방지 안전장치**. 아래 3절 참고 |
| ONNX Threads | ONNX 3종 | 1 | 스트리밍 모델이라 1~2가 보통 가장 빠름 |
| DPDFNet 변형 | DPDFNet | baseline | baseline(8.4MB) / 2 / 4 / 8(14.6MB) |
| Min Gain (dB) | NSNet2 + DSP | NSNet2 −80, DSP −20~−25 | 이보다 더 깎지 않음 |
| alpha | DSP | MMSE·Wiener 0.98 / SS 4.0 | DD 평활 계수 또는 과감산 계수 |
| beta | Spectral Subtraction | 0.02 | 스펙트럼 바닥 |
| Noise Quantile (%) | DSP | 10 | 조용한 하위 N% 프레임으로 잡음 추정 |

---

## 2. VAD 는 Silero 로 통일했습니다 — 판단 근거

"자체 VAD 가 있으면 그것을, 없으면 Silero, 아니면 통일해도 좋다. 네가 판단해라" 하셨습니다.
**Silero 로 통일**이 맞다고 판단했습니다.

1. **실행 가능한 4종(GTCRN / DPDFNet / NSNet2 / 전통 DSP) 중 쓸 만한 자체 VAD 를 가진 것이 하나도 없습니다.**
   전부 순수 노이즈 억제 모델입니다.
2. **자체 VAD 를 가진 유일한 후보인 WebRTC APM 은 애초에 이 앱에서 실행이 안 됩니다.**
   (그리고 현재 WebRTC APM 의 `AudioProcessing::Config` 에는 `VoiceDetection` 구조체가 **제거**되어 없습니다.
   `common_audio/vad` 의 독립 VAD 는 APM 의 일부가 아니고 APM 을 통해 노출되지도 않습니다)
3. **가장 중요한 이유 — 벤치마크가 깨집니다.** 알고리즘마다 VAD 가 다르면 WER/CER 차이가
   노이즈 저감 때문인지 VAD 때문인지 구분할 수 없습니다. **변수를 하나만 바꾸기 위해** VAD 를 고정했습니다.

### 파이프라인 순서

```
디코딩 → [Noise Reduction] → [Silero VAD] → STT
```

첨부하신 조사 문서 5장의 권장 순서와 같습니다. **VAD 는 노이즈 저감을 끝낸 신호를 봅니다** — 잡음을 먼저 없애야 음성 구간 판정이 정확해지기 때문입니다.

---

## 3. ★ Attenuation Limit — 조사 문서의 원칙 1번을 코드로 강제

첨부 문서의 "**과도한 노이즈 제거 금지 — 강한 NS 는 아티팩트로 WER 악화 가능**" 을 옵션이 아니라 **구조로** 넣었습니다.

```
lim = 10^(-attenuationLimitDb / 20)
출력 = lim × 원음 + (1 − lim) × 처리음
```

즉 "출력이 원음보다 **최대 몇 dB 까지만** 작아지게" 원음을 섞습니다.
DeepFilterNet 의 `atten_lim_db` 와 같은 방식이고, sherpa-onnx 는 DPDFNet 에 12 dB 를 씁니다.

**왜 필요한가**: 실측에서 DPDFNet 은 잡음 구간을 **−95 dB 까지** 깎아버렸습니다.
이 정도면 사람 귀에는 깨끗하지만 STT 엔진에는 부자연스러운 신호가 되어 오히려 WER 이 나빠질 수 있습니다.
딥러닝 계열 기본값을 12 dB 로 둔 이유입니다.

**WER 이 나빠졌다면 이 값을 가장 먼저 의심하세요.** 12 → 6 dB 로 낮추면 원음이 더 많이 남습니다.

---

## 4. 단계별 소요 시간 계측 — 요청하신 부분

`result.csv` 에 컬럼 17개를 추가했습니다(기존 62개 **뒤에** 붙여서 index 는 그대로입니다).

```
ns_prepare_ms → ns_stft_ms → ns_infer_ms → ns_istft_ms → ns_post_ms  =  ns_total_ms
```

**모든 알고리즘이 같은 STFT 루프를 타도록** 공통 뼈대(`SpectralNoiseReducer`)를 만들었습니다.
딥러닝이든 전통 DSP 든 같은 방식으로 시간이 측정되므로 **알고리즘 간 비교가 공정합니다.**

### ★ 속도 비교는 `ns_infer_per_frame_us` 로 하세요

`ns_total_ms` 는 파일 길이와 hop 크기에 좌우됩니다. 같은 85초 파일이라도 프레임 수가 다릅니다.

| 알고리즘 | n_fft / hop | 85초 파일 프레임 수 |
|---|---|---|
| GTCRN | 512 / 256 | 약 5,300 |
| DPDFNet, NSNet2 | 320 / 160 | 약 8,500 |
| 전통 DSP | 512 / 256 | 약 5,300 |

프레임당 시간으로 정규화해야 "어느 모델이 무거운가"가 제대로 드러납니다.

### 참고 실측치 (컨테이너 CPU 1스레드 기준, 휴대폰은 다릅니다)

| 알고리즘 | RTF | 85초 파일 예상 |
|---|---|---|
| GTCRN | 0.053 | 약 4.5 초 |
| DPDFNet baseline | 0.096 | 약 8.2 초 |
| DPDFNet 8 | 0.371 | 약 32 초 |

---

## 5. 검증한 것

### 5-1. 실제 모델 파일을 열어서 확인 (추측 아님)

| 항목 | 확인 결과 |
|---|---|
| GTCRN I/O | `mix[1,257,1,2]`, `conv_cache[2,1,16,16,33]`, `tra_cache[2,3,1,1,16]`, `inter_cache[2,1,33,16]` → `enh` + cache 3종 |
| GTCRN 메타 | `n_fft=512, hop=256, window_type=hann_sqrt, sample_rate=16000` (파일 안에 박혀 있음) |
| GTCRN sha256 | `e77603ac…b534` — sherpa-onnx `checksum.txt` 와 일치 |
| DPDFNet I/O | `spec[1,1,161,2]`, `state_in[38256]` → `spec_e`, `state_out` |
| DPDFNet 메타 | `n_fft=320, hop=160, window_type=vorbis, center=1, pad_mode=reflect`, `erb_norm_init`(32개), `spec_norm_init`(96개) |
| NSNet2 파이프라인 | 공식 `enhance_onnx.py` / `featurelib.py` 원문에서 확인: `N_fft=320, N_hop=160, sqrt(대칭 Hann)`, `log10(max(|Y|²,1e-12))`, gain 을 `[10^(mingain/20), 1.0]` 로 clip 후 **복소** 스펙트럼에 곱함(위상 보존) |

### 5-2. DPDFNet state 초기화 — 실측으로 결정

`state_in` 을 어떻게 채우느냐로 성능이 갈립니다. 실제 음원으로 세 가지를 다 돌려봤습니다.

| 초기화 | 잡음 구간 감쇠 |
|---|---|
| 전부 0 | 52.7 dB |
| **`erb_norm_init` @0 + `spec_norm_init` @32** | **67.7 dB** ← 채택 |

sherpa-onnx C++ 구현과도 일치합니다. 15 dB 차이라 그냥 0 으로 채우면 안 됩니다.

### 5-3. 직접 구현한 신호처리 — numpy 대조

| 검증 | 결과 |
|---|---|
| FFT (radix-2 + Bluestein) vs `numpy.fft` | n = 512/320/1024/256/640/960/480/100/7/1 **전부 상대오차 1e-15** ✔ |
| FFT 역변환 왕복 | 오차 3e-15 ✔ |
| sqrt-Hann / Vorbis 창의 창² OLA 합 | **정확히 1.000000000** ✔ |
| STFT→iSTFT 항등 재구성 | 최대오차 **1.7e-16** ✔ |
| Bessel `i0e`/`i1e` (MMSE 용) | MMSE 가 실제로 쓰는 구간(u ≤ 0.5)에서 **오차 2.5e-8** ✔, 큰 x 에서 overflow 없음 ✔ |
| MMSE 근사식 vs 정확식 이음매 | v > 1 구간 최대 **0.034 dB** (문헌 0.02 dB 수준) ✔ |

> **왜 FFT 를 직접 짰나**: DPDFNet 과 NSNet2 의 `n_fft` 가 **320** 인데 2의 거듭제곱이 아닙니다.
> 512 로 zero-pad 하면 주파수 bin 이 161 → 257 로 바뀌어 모델 입력 규격이 깨집니다.
> 그래서 320-point FFT 를 그대로 계산하는 Bluestein 경로를 넣었습니다.
> (전통 DSP 는 512/256 을 써서 빠른 radix-2 경로를 탑니다)

### 5-4. 정합성 검사 (정적)

| 검사 | 결과 |
|---|---|
| `R.id.*` 참조 30개 ↔ 레이아웃 | 30/30 ✔ |
| `@string/*` 참조 51개 ↔ `strings.xml` | 51/51 ✔ |
| `CSV_HEADER` 79개 ↔ `toCsvLine()` 79개 | 일치 ✔ |
| **기존 62개 컬럼 순서** | 원본과 **완전 동일** ✔ |
| `row.*` 대입 52개 ↔ `SttRow` 필드 74개 | 전부 존재 ✔ |
| denoise 패키지 심볼 13종 | 전부 정의됨 ✔ |
| Kotlin 9개 파일 괄호 균형 | 전부 균형 ✔ |

### 5-5. ★ 검증하지 못한 것

| 항목 | 이유 |
|---|---|
| **실제 컴파일** | 이 작업 환경에 Android SDK / Kotlin 컴파일러가 없습니다 |
| **NSNet2 실제 추론** | 모델이 `raw.githubusercontent.com` 에 있는데 이 환경에서 403 으로 막혀 내려받지 못했습니다. 공식 스크립트 원문으로 파이프라인을 맞췄고, **입출력 이름은 하드코딩하지 않고 세션에서 직접 읽습니다**(공식 스크립트와 같은 방식) |
| **휴대폰에서의 실제 속도** | 위 RTF 는 컨테이너 CPU 값입니다. 특히 GTCRN 은 프레임당 연산이 매우 작아 **JNI 호출 오버헤드가 지배적**이라, 실제 폰에서는 표보다 빠를 가능성이 큽니다 |
| **WER/CER 개선 여부** | 이건 원래 실측으로만 답할 수 있습니다 |

---

## 5-6. 빌드 에러 수정 (add-hyungchul-20260828-1000)

### 보고해 주신 에러 4개의 진짜 원인

```
Unresolved reference 'NoiseReducerFactory'.            ← MainActivity.kt:91 (import)
Cannot infer type for type parameter 'T'.
Unresolved reference 'NoiseReducerFactory'.
Unresolved reference 'label'.
```

**4개가 아니라 1개입니다.** 아래 3개는 첫 번째의 연쇄 결과입니다.

```
NoiseReducerFactory 미해결
   → NoiseReducerFactory.create(...) 미해결
   → withContext(Dispatchers.Default) { create(...) } 의 T 추론 실패   ← 2번 에러
   → reducer 타입 불명 → reducer.label 미해결                          ← 4번 에러
```

### 왜 그 심볼 하나만 안 잡히는가

이번에는 **Kotlin 2.2.20 컴파일러를 실제로 받아서 컴파일해 봤습니다.** 결과는 **에러 0개 / 경고 0개**였습니다.
`MainActivity` 가 하는 것과 똑같은 사용 패턴(`create` 호출 → `withContext` → `.label` → `buildList`)으로
재현 코드를 짜서 컴파일해도 **에러가 나지 않습니다.**

그런데 같은 파일 안의 다른 심볼 5개는 잡히고 `NoiseReducerFactory` 하나만 안 잡힙니다.
`NoiseReducer.kt` 안의 선언 위치를 보면 답이 나옵니다.

| 줄 | 선언 | 해결 여부 |
|---|---|---|
| 37 | `NoiseAlgorithmKind` | ✅ |
| 55 | `NoiseAlgorithm` | ✅ |
| 158 | `NoiseReduceConfig` | ✅ |
| 328 | `NoiseReducer` | ✅ |
| 367 | `PcmUtil` (428줄에서 끝남) | — |
| **431** | **`NoiseReducerFactory`** | ❌ |

**파일이 428줄 근처에서 잘리면 정확히 이 증상이 납니다.**
`PcmUtil` 이 끝나는 지점이라 괄호도 맞아서 문법 에러조차 안 나고, 조용히 마지막 선언만 사라집니다.

### 확인 방법

```bash
wc -l app/src/main/java/com/example/google_stt/denoise/NoiseReducer.kt
#  → 459 가 나와야 합니다
grep -n "object NoiseReducerFactory" app/src/main/java/com/example/google_stt/denoise/NoiseReducer.kt
#  → 431 이 나와야 합니다
```

또는 한 번에:

```bash
bash tools/verify_sources.sh
```

파일이 잘렸으면 `[잘림!]` 으로 표시됩니다. Beyond Compare 로 손수 병합하실 때
파일 끝이 누락되는 사고를 미리 잡으려고 넣었습니다.

### 경고 7개

| 경고 | 처리 |
|---|---|
| `Enum.values()` → `entries` (4개) | 고쳤습니다. `values()` 는 호출마다 배열을 새로 만들고, `entries` 는 불변 List 라 권장값입니다 |
| `Parameter "e" is never used` (2개) | `catch (_: IOException)` / `catch (_: InterruptedException)` 로 고쳤습니다. 원래 코드에 있던 것입니다 |
| `Initializer is redundant` (1개) | **오탐입니다.** `var sessionStartNs = 0L` 은 try 블록 안에서만 대입돼서, 초기값을 지우면 Kotlin 의 확정 대입 검사를 통과하지 못해 컴파일이 안 됩니다. 주석으로 근거를 남겨 뒀습니다 |

---

## 6. 지금 해야 할 일

### 6-1. 모델 받기

```bash
cd ~/workspace/AndroidStudioProjects/2026/google_stt
bash tools/download_models.sh          # Silero VAD + GTCRN + DPDFNet baseline
bash tools/download_models.sh all      # 위 + NSNet2 + DPDFNet 2/4/8
```

받은 뒤 **Android Studio 에서 다시 빌드**하세요(assets 는 APK 에 구워집니다).

> `gtcrn_simple.onnx` 는 **zip 에 이미 넣어 뒀습니다**(523 KB, MIT).
> 아무것도 안 받아도 GTCRN 과 전통 DSP 3종은 바로 돌아갑니다.
> 지난번에 못 받으셨던 **Silero VAD 모델도 이 스크립트가 같이 받습니다.**

### 6-2. 저장소에 빠진 것 하나

`app/build.gradle.kts` 에 **onnxruntime 의존성이 커밋되어 있지 않습니다**(로컬에만 있으신 것 같습니다).
이번 zip 의 `app/build.gradle.kts` 가 그 누락을 포함한 최종본입니다. `STT_계측_CSV_설명서.md` 도 지난번 갱신분이 커밋되지 않아 이번에 함께 반영했습니다.

---

## 7. 권장 실험 순서

같은 음원 24개 / 같은 모드(`base`) / 같은 Feed(`D10`) / **VAD 는 ASR Safe 로 고정**하고, 노이즈 저감만 바꿉니다.

| # | Noise Reduction | 출력 폴더 |
|---|---|---|
| 1 | 사용 안 함 | `output/google/base/vad_asr_safe/` |
| 2 | MMSE-STSA | `.../ns_mmse/vad_asr_safe/` |
| 3 | GTCRN (limit 12) | `.../ns_gtcrn_lim12/vad_asr_safe/` |
| 4 | GTCRN (limit 6) | `.../ns_gtcrn_lim6/vad_asr_safe/` |
| 5 | DPDFNet baseline (limit 12) | `.../ns_dpdfnet_baseline_lim12/vad_asr_safe/` |

그리고 이 5개를 놓고 봅니다.

1. `ns_reduction_db` — 실제로 얼마나 깎았나
2. `ns_infer_per_frame_us` — 알고리즘 비용
3. `ns_peak` — **1.0 을 넘으면 클리핑**. WER 악화 원인이 품질이 아니라 클리핑일 수 있습니다
4. `total_rtf` — end-to-end 로 감당 가능한가
5. **`.txt` 를 정답과 대조한 WER/CER** — 최종 판정

### 3번과 4번을 같이 넣은 이유

첨부 문서의 원칙 그대로, **NS 강도가 과하면 WER 이 나빠질 수 있습니다.**
같은 알고리즘을 limit 12 / 6 두 조건으로 돌려 보면 "얼마나 깎는 게 최적인가"가 한 번에 나옵니다.

### 이전에 발견한 함정 하나

`customer/04`(112자, 중앙값 587자)와 `friend/04`(19자, 중앙값 436자)는 음원 앞부분을 통째로 잃었고,
이는 100 / 15 / 10 ms 모두에서 재현된 **엔진의 세션·endpointing 결함**입니다.
**노이즈 저감으로 이 두 파일이 좋아지는지 반드시 따로 확인하세요.**

---

## 8. 변경 파일 목록

| 파일 | 상태 |
|---|---|
| `denoise/AudioDsp.kt` | **신규** — FFT(radix-2 + Bluestein), 창 함수, STFT/iSTFT |
| `denoise/NoiseReducer.kt` | **신규** — 인터페이스, 알고리즘 enum(불가 사유 포함), 설정, 결과, 팩토리 |
| `denoise/SpectralNoiseReducer.kt` | **신규** — 공통 프레임 루프 + 단계별 시간 계측 + 감쇠 제한 |
| `denoise/OnnxNoiseReducer.kt` | **신규** — GTCRN / DPDFNet / NSNet2 |
| `denoise/DspNoiseReducer.kt` | **신규** — MMSE-STSA / Wiener / Spectral Subtraction + Bessel |
| `MainActivity.kt` | 수정 — 알고리즘 UI, 파이프라인 단계 추가, 출력 폴더 조합 |
| `SttTelemetry.kt` | 수정 — NS 컬럼 17개를 맨 뒤에 append, `total_ms` 확장 |
| `res/layout/activity_main.xml` | 수정 — 알고리즘 스피너 + 옵션 UI (순수 추가) |
| `res/values/strings.xml` | 수정 — NS 문자열 9개 (순수 추가) |
| `app/build.gradle.kts` | 수정 — **onnxruntime 1.25.1 추가**(저장소에 누락되어 있었음) |
| `app/src/main/assets/gtcrn_simple.onnx` | **신규** — GTCRN 모델 (MIT, 523 KB) |
| `app/src/main/assets/README_MODELS.md` | **신규** |
| `tools/download_models.sh` | **신규** — 모델 일괄 다운로드 |
| `STT_계측_CSV_설명서.md` | 수정 — VAD 18컬럼 + NS 17컬럼 설명, 비교용 pandas 예제 |

`AudioDecoder.kt`, `SileroVadProcessor.kt`, `AndroidManifest.xml`, `themes.xml`, `colors.xml`, gradle wrapper 는
**손대지 않았습니다**(SileroVadProcessor 는 다운로드 스크립트 이름 한 줄만 바뀌었습니다).

---

## 9. 참고 (2026-08-26 확인)

- GTCRN — https://github.com/Xiaobin-Rong/gtcrn (MIT)
- DPDFNet — https://github.com/ceva-ip/DPDFNet (Apache-2.0)
- 모델 배포 — https://github.com/k2-fsa/sherpa-onnx/releases/tag/speech-enhancement-models
- NSNet2 — https://github.com/microsoft/DNS-Challenge/tree/icassp2021-final/NSNet2-baseline (MIT)
- Ephraim & Malah 1984 — https://malah.net.technion.ac.il/files/2017/08/Ephraim_Speech_Enhancement_ASSP84.pdf
- VOICEBOX `v_ssubmmse` — http://www.ee.ic.ac.uk/hp/staff/dmb/voicebox/mdoc/v_mfiles/v_ssubmmse.html
- NoiseSuppressor — https://developer.android.com/reference/android/media/audiofx/NoiseSuppressor
- WebRTC APM — https://webrtc.googlesource.com/src/+/HEAD/api/audio/audio_processing.h
