# Android STT 노이즈 저감 파이프라인 구현 프롬프트

> 이 문서는 **다른 AI(코드 생성 어시스턴트)에게 전달**하여 Android 애플리케이션 코드를 작성하게 하기 위한 프롬프트입니다.
> 함께 전달할 참고 문서: `노이즈저감_알고리즘_정리_20260826.md`
> 작성일: 2026-08-26

---

## 0. 역할 지시 (System / Role)

당신은 **Android 온디바이스 오디오·STT 파이프라인 전문 개발자**입니다.
Kotlin, AudioRecord, ONNX Runtime Mobile, JNI/NDK, ML Kit GenAI에 능숙합니다.
아래 요구사항과 첨부된 알고리즘 정리 문서(`노이즈저감_알고리즘_정리_20260826.md`)를 근거로,
**동작 가능한 실제 코드**를 작성하세요. 임의로 라이브러리를 바꾸지 말고, 불명확한 부분은 가정을 명시하세요.

---

## 1. 프로젝트 컨텍스트

- **앱 목적**: 대화/회의 음성을 STT로 전사 → 회의록·대화 요약·하루 기록·일정/약속 잡기·스케줄 반영
- **패키지**: `com.example.google_stt`
- **메인 파일**: `app/src/main/java/com/example/google_stt/MainActivity.kt`
- **STT 엔진**: **Google Embedded — ML Kit GenAI Basic (API 31+, on-device)**
- **오디오 처리 단위**: **10초 세그먼트 배치** (초저지연 불필요, 세그먼트 품질 우선)
- **최소 SDK**: API 31 (Android 12) 이상
- **언어/빌드**: Kotlin + Gradle Kotlin DSL (`build.gradle.kts`)

### 소음 환경
카페 소음(babble), 도로 소음, 음악 소음, 키보드 타자음, 마우스 클릭음 → 대부분 **비정상(non-stationary) 소음**

---

## 2. 구현 목표 (우선순위 순)

첨부 문서의 권장 우선순위를 그대로 따릅니다.

1. **1순위 주력: GTCRN (ONNX Runtime, 16kHz)** — 노이즈 저감 메인
2. **2순위 대안: DeepFilterNet (ONNX Runtime, 48kHz)** — 런타임 교체 가능한 구조로 설계
3. **전처리/무음 필터: WebRTC APM 또는 Silero VAD** — 비음성 10초 세그먼트는 STT 호출 스킵
4. **최소 방어선: 내장 NoiseSuppressor + `MediaRecorder.AudioSource.VOICE_RECOGNITION`**

> **중요**: 노이즈 저감 모듈은 **인터페이스로 추상화**하여 GTCRN / DeepFilterNet / (무처리)를 런타임에 교체 가능하게 하세요.

---

## 3. 필수 아키텍처 요구사항

### 3.1 파이프라인 구조
```
AudioRecord(VOICE_RECOGNITION, 16kHz, mono, PCM16)
   → [내장 NoiseSuppressor (옵션, 토글)]
   → NoiseReducer 인터페이스 (GTCRN | DeepFilterNet | NoOp)
   → VAD (Silero 또는 WebRTC, 무음 세그먼트 스킵)
   → 10초 세그먼트 버퍼
   → ML Kit GenAI Basic STT
   → 후처리(요약/일정 추출 훅 인터페이스만)
```

### 3.2 인터페이스 설계 (반드시 포함)
```kotlin
interface NoiseReducer {
    val requiredSampleRate: Int      // 예: GTCRN=16000, DFN=48000
    fun process(pcm: ShortArray): ShortArray   // 프레임 단위 처리
    fun close()
}

interface VoiceActivityDetector {
    fun isSpeech(pcm: ShortArray): Boolean
    fun close()
}

interface SttEngine {
    suspend fun transcribe(pcm16k: ShortArray): String
}
```

- `GtcrnNoiseReducer`, `DeepFilterNetNoiseReducer`, `NoOpNoiseReducer` 3개 구현
- 샘플레이트 불일치 시 **리샘플러**(16k↔48k) 유틸 포함
- 모든 오디오 버퍼는 16-bit PCM mono 기준

### 3.3 스레딩/성능
- 오디오 캡처와 추론을 **별도 스레드/코루틴**으로 분리 (녹음 드롭 방지)
- ONNX Runtime은 **NNAPI 또는 XNNPACK Execution Provider** 사용 옵션 제공
- 10초 세그먼트는 링버퍼로 관리, 백프레셔 처리

---

## 4. 기술 스택 / 의존성

```kotlin
// app/build.gradle.kts
dependencies {
    implementation("com.microsoft.onnxruntime:onnxruntime-android:1.20.0")
    // ML Kit GenAI (실제 아티팩트명은 공식 문서 확인 후 명시할 것)
    // Silero VAD ONNX 모델 사용 시 위 onnxruntime 재사용
    implementation("org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.1")
}
```

- 모델 파일 배치: `app/src/main/assets/` (`gtcrn.onnx`, `deepfilternet.onnx`, `silero_vad.onnx`)
- WebRTC APM을 쓸 경우 NDK/CMake + JNI 구조도 별도 제시

---

## 5. 산출물 요구사항 (Deliverables)

다음을 **모두** 코드로 제공하세요.

1. `build.gradle.kts` 의존성 및 `assets` 설정 diff
2. `NoiseReducer` 인터페이스 + `GtcrnNoiseReducer`(ONNX 로딩·STFT/전후처리 포함) 구현
3. `DeepFilterNetNoiseReducer` 스텁 또는 구현(교체 가능 구조)
4. `NoOpNoiseReducer`
5. `SileroVadDetector`(ONNX) 구현
6. `AudioResampler`(16k↔48k) 유틸
7. `AudioCaptureManager`(AudioRecord + VOICE_RECOGNITION + 내장 NoiseSuppressor 토글 + 10초 세그먼트화)
8. `MlKitSttEngine`(ML Kit GenAI Basic 래퍼)
9. `MainActivity.kt` 통합 예시(권한 요청, 파이프라인 연결, UI에 전사 텍스트 표시)
10. 각 모듈 단위의 사용 예시와 주석

---

## 6. 반드시 지킬 제약/원칙

1. **과도한 노이즈 제거 금지**: ML Kit STT는 잡음에 어느 정도 강건. 강한 NS는 아티팩트로 WER 악화 가능 → NS on/off 및 강도 **토글/설정값**을 노출할 것.
2. **중복 NS 주의**: 내장 NoiseSuppressor + 딥러닝 NS 동시 적용은 음성 뭉개짐 유발 가능 → 기본은 **택일**, 설정으로 병용 가능하게.
3. **샘플레이트 일치 우선**: 기본 경로는 16kHz(GTCRN)로 리샘플 최소화.
4. **VAD로 비용 절감**: 무음/비음성 10초 세그먼트는 STT 호출을 스킵.
5. **에러/리소스 관리**: ONNX 세션, AudioRecord, 코루틴 스코프의 생명주기(`onDestroy`) 정리.
6. **권한**: `RECORD_AUDIO` 런타임 권한 처리 포함.
7. **가정 명시**: ML Kit GenAI Basic의 실제 API 시그니처가 불확실하면, 인터페이스로 감싸고 TODO/가정을 주석으로 명확히 표기.

---

## 7. 코드 스타일

- Kotlin idiomatic, 코루틴(`Dispatchers.IO`/`Default`) 사용
- 명확한 KDoc 주석(한국어 가능)
- 하드코딩 최소화, 설정은 `data class Config`로 분리
- 예외 안전(try/finally로 native/onnx 리소스 해제)

---

## 8. 출력 형식 지시

- 파일별로 **경로 헤더 + 전체 코드 블록**으로 제시
- 마지막에 **통합 테스트 시나리오**(마이크 → 전사)와 **WER A/B 테스트 방법**(원음 vs NS vs NS+VAD) 간단 가이드 포함
- 불확실한 외부 API는 "가정" 섹션으로 분리

---

## 9. 첨부 문서 활용 지침

`노이즈저감_알고리즘_정리_20260826.md`를 근거 자료로 사용하세요:
- 알고리즘 선택 근거(성능/난이도/입력 샘플레이트/VAD 포함 여부)는 해당 문서의 비교표를 참조
- 우선순위(GTCRN → DeepFilterNet → WebRTC APM → 내장 NS)는 문서의 4장을 그대로 따를 것
- 파이프라인 순서는 문서의 5장을 기준으로 구현

---

## 10. 완료 기준 (Definition of Done)

- [ ] GTCRN 경로로 마이크 입력 → 노이즈 저감 → 10초 세그먼트 → ML Kit STT 전사가 동작
- [ ] NoiseReducer 런타임 교체(GTCRN/DFN/NoOp) 가능
- [ ] VAD로 무음 세그먼트 스킵 동작
- [ ] 내장 NoiseSuppressor 토글 동작
- [ ] 리소스 누수 없이 생명주기 정리
- [ ] 설정값(NS on/off, EP 선택, VAD 임계값)이 외부에서 조정 가능
